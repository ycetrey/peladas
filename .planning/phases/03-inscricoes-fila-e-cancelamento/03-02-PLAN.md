---
phase: 03-inscricoes-fila-e-cancelamento
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - apps/api/src/registrations
  - apps/api/src/app.module.ts
  - README.md
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - API-04
  - API-05
user_setup: []
must_haves:
  truths:
    - "POST .../registrations cria CONFIRMED ou SUBSTITUTE conforme vagas"
    - "DELETE .../me cancela e promove primeiro reserva quando titular CONFIRMED cancela"
  artifacts:
    - apps/api/src/registrations/registrations.controller.ts
    - apps/api/src/registrations/registrations.e2e-spec.ts
  key_links:
    - "03-CONTEXT.md D-01 cabeçalho jogador"
---

<objective>
HTTP **inscrever** e **cancelar** com `PlayerUserGuard`, serviço Prisma com **$transaction** na promoção, e2e e README.

Purpose: Fechar REG-* e API-04/05.

Output: `RegistrationsModule`, e2e, documentação.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-inscricoes-fila-e-cancelamento/03-CONTEXT.md
@apps/api/src/matches/organizer-user.guard.ts
@apps/api/src/matches/matches.service.ts
@apps/api/src/prisma/prisma.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Player guard + module + DTO</name>
  <files>apps/api/src/registrations/player-user.guard.ts, apps/api/src/registrations/request-with-player.ts, apps/api/src/registrations/dto/register-player.dto.ts, apps/api/src/registrations/registrations.module.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - `apps/api/src/matches/organizer-user.guard.ts`
    - `apps/api/src/matches/request-with-organizer.ts`
  </read_first>
  <action>
    `PlayerUserGuard` espelha `OrganizerUserGuard` mas grava `req.playerUser` (`User` Prisma) a partir de `x-player-user-id`.
    `RequestWithPlayer` tipo auxiliar.
    `RegisterPlayerDto` com `preferredPosition: PlayerPosition` (`@IsEnum`).
    `RegistrationsModule` importa `PrismaModule`, declara providers placeholder para `RegistrationsService` (implementação na tarefa 2), `RegistrationRulesService`, `PlayerUserGuard`; exportar o necessário.
    Importar `RegistrationsModule` em `AppModule`.
  </action>
  <acceptance_criteria>
    - `player-user.guard.ts` e `registrations.module.ts` existem
    - `app.module.ts` importa `RegistrationsModule`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec nest build</automated>
  </verify>
  <done>Infra HTTP preparada.</done>
</task>

<task type="auto">
  <name>Task 2: RegistrationsService + controller POST/DELETE</name>
  <files>apps/api/src/registrations/registrations.service.ts, apps/api/src/registrations/registrations.controller.ts</files>
  <read_first>
    - `apps/api/src/registrations/registration-rules.service.ts`
    - `apps/api/prisma/schema.prisma`
    - `apps/api/src/common/errors/domain-errors.ts`
  </read_first>
  <action>
    `RegistrationsController` com `@Controller('matches/:matchId/registrations')`: `@Post()` com `PlayerUserGuard`; `@Delete('me')` com `PlayerUserGuard`.
    `RegistrationsService.register(matchId, userId, dto)`:
    - Carregar `Match` por id; `rules.assertMatchOpenForRegistration(match, new Date())`.
    - Procurar inscrição ativa `(CONFIRMED|SUBSTITUTE)` para `(matchId,userId)`; `rules.assertNoActiveDuplicate(...)`.
    - Contar confirmados e reservas ativos; `decideStatusForNewRegistration`; calcular `queueOrder` = max+1 no match.
    - `prisma.registration.create`.
    `cancelMine(matchId, userId)`:
    - Encontrar inscrição ativa do user para o match; se não existir 404 (`MatchNotFoundError` ou erro dedicado — escolher `MatchNotFoundError` com mensagem "Registration not found" **ou** novo `RegistrationNotFoundError` documentado; preferir consistência: usar `InvalidMatchStateError` se não houver registo ativo — documentar escolha no README).
    - Se `CONFIRMED` titular: `prisma.$transaction`: update self `CANCELED`; `findFirst` `SUBSTITUTE` orderBy `queueOrder asc`; se existir, `update` para `CONFIRMED`.
    - Se `SUBSTITUTE`: apenas marcar `CANCELED`.
    Respostas JSON mínimas `{ registration: {...} }` ou `{ success: true }` no cancel — documentar.
  </action>
  <acceptance_criteria>
    - `registrations.service.ts` contém `$transaction` e `registration.create`
    - `registrations.controller.ts` contém `@Controller('matches/:matchId/registrations')`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec nest build</automated>
  </verify>
  <done>REG-01..03, API-04, API-05.</done>
</task>

<task type="auto">
  <name>Task 3: E2E + README</name>
  <files>apps/api/src/registrations/registrations.e2e-spec.ts, README.md</files>
  <read_first>
    - `apps/api/src/matches/matches.e2e-spec.ts`
    - `apps/api/prisma/seed.ts`
    - `README.md`
  </read_first>
  <action>
    E2E (`registrations.e2e-spec.ts`): com `DATABASE_URL`, criar partida `OPEN` via `POST /matches` (organizer seed `000...001`), inscrever jogador 1 e 2 com `X-Player-User-Id` adequados até preencher titulares (ajustar `maxPlayers` pequeno ex. 2) e pelo menos um reserva se `maxSubstitutes>0`; depois `DELETE` titular e assert que ex-reserva fica `CONFIRMED`. Limpar dados (`deleteMany` por prefixo de título `E2E-REG-` ou ids rastreados).
    README: secção **Inscrições** com exemplos `curl` para `POST /matches/{id}/registrations` e `DELETE /matches/{id}/registrations/me` com `X-Player-User-Id`.
  </action>
  <acceptance_criteria>
    - `registrations.e2e-spec.ts` referencia `/registrations` ou `registrations`
    - `README.md` contém `X-Player-User-Id` e `registrations`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec jest registrations.e2e-spec.ts</automated>
  </verify>
  <done>Smoke + docs.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Player header | Só utilizadores existentes; sem partilha de sessão |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | E | Cancel+promote | mitigate | Transação atómica; teste e2e cobre ordem |
| T-03-03 | I | Path matchId | mitigate | Validar UUID de match existente antes de criar |
</threat_model>

<verification>
pnpm --filter @peladas/api exec nest build
pnpm --filter @peladas/api exec jest registrations.e2e-spec.ts
</verification>

<success_criteria>
E2E verde com Postgres; README atualizado.
</success_criteria>

<output>
Após conclusão: `.planning/phases/03-inscricoes-fila-e-cancelamento/03-02-SUMMARY.md`
</output>

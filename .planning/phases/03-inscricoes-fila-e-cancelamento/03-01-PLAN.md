---
phase: 03-inscricoes-fila-e-cancelamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/common/errors/domain-errors.ts
  - apps/api/src/registrations/registration-rules.service.ts
  - apps/api/src/registrations/registration-rules.service.spec.ts
autonomous: true
requirements:
  - ERR-01
  - RULE-02
  - RULE-03
user_setup: []
must_haves:
  truths:
    - "NoRegistrationSlotsError extends DomainError com code estável"
    - "RegistrationRulesService valida janela OPEN e slots titular/reserva"
  artifacts:
    - apps/api/src/registrations/registration-rules.service.ts
    - apps/api/src/registrations/registration-rules.service.spec.ts
  key_links:
    - "03-CONTEXT.md D-04–D-08"
---

<objective>
Estender erros de domínio e implementar **RegistrationRulesService** testável (RULE-02, RULE-03), preparando serviço HTTP da vaga 03-02.

Purpose: Isolar regras antes de Prisma write complexo.

Output: `NoRegistrationSlotsError`, serviço + Jest.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-inscricoes-fila-e-cancelamento/03-CONTEXT.md
@.planning/intel/constraints.md
@apps/api/prisma/schema.prisma
@apps/api/src/common/errors/domain-errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: NoRegistrationSlotsError</name>
  <files>apps/api/src/common/errors/domain-errors.ts</files>
  <read_first>
    - `apps/api/src/common/errors/domain-errors.ts`
    - `.planning/intel/constraints.md` (C-SPEC-005)
  </read_first>
  <action>
    Adicionar classe `NoRegistrationSlotsError extends DomainError` com `readonly code = "NoRegistrationSlotsError"` e mensagem default explícita (sem vaga para titular nem reserva).
    Garantir exportação pública ao lado dos outros erros.
  </action>
  <acceptance_criteria>
    - `domain-errors.ts` contém a string `NoRegistrationSlotsError`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec nest build</automated>
  </verify>
  <done>ERR-01 estendido com código nomeado para lotação.</done>
</task>

<task type="auto">
  <name>Task 2: RegistrationRulesService + unit tests</name>
  <files>apps/api/src/registrations/registration-rules.service.ts, apps/api/src/registrations/registration-rules.service.spec.ts</files>
  <read_first>
    - `apps/api/prisma/schema.prisma` (`Match`, `Registration`, enums)
    - `.planning/intel/constraints.md` (C-SPEC-007 fragmento inscrição, C-SPEC-008)
    - `apps/api/src/common/errors/domain-errors.ts`
  </read_first>
  <action>
    Criar `RegistrationRulesService` com métodos públicos claros, por exemplo:
    - `assertMatchOpenForRegistration(match, now)` — lança `MatchNotOpenError` se `status !== OPEN`; `RegistrationClosedError` se `now` fora de `[registrationOpensAt, registrationClosesAt]`.
    - `assertNoActiveDuplicate(activeRegistration)` — lança `UserAlreadyRegisteredError` se argumento não nulo.
    - `decideStatusForNewRegistration(confirmedTitularCount, substituteCount, maxPlayers, maxSubstitutes)` — devolve `CONFIRMED` ou `SUBSTITUTE` ou lança `NoRegistrationSlotsError`.
    O serviço não acede a Prisma; recebe números/`Match` já carregados.
    Specs: janela válida não lança; fora da janela lança `RegistrationClosedError`; duplicate lança `UserAlreadyRegisteredError`; quando titulares cheios e reservas cheias lança `NoRegistrationSlotsError`; quando titulares cheios mas há reserva livre devolve `SUBSTITUTE`.
  </action>
  <acceptance_criteria>
    - Ambos os ficheiros em `<files>` existem
    - `registration-rules.service.ts` referencia `MatchNotOpenError` e `NoRegistrationSlotsError`
    - `registration-rules.service.spec.ts` tem pelo menos 4 blocos `it(`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec jest registration-rules.service.spec.ts</automated>
  </verify>
  <done>RULE-02 e RULE-03 cobertos por testes unitários.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Rules only | Sem persistência; sem dados sensíveis |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | Rule drift | mitigate | Testes cobrem casos limite de contagens |
</threat_model>

<verification>
pnpm --filter @peladas/api exec jest registration-rules.service.spec.ts
pnpm --filter @peladas/api exec nest build
</verification>

<success_criteria>
Jest verde para regras; build OK.
</success_criteria>

<output>
Após conclusão: `.planning/phases/03-inscricoes-fila-e-cancelamento/03-01-SUMMARY.md`
</output>

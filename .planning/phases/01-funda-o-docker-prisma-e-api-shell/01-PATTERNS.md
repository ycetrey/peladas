# Phase 1 — Pattern map

**Status:** Greenfield — no application code analogs yet.

## Reusable assets

- None in repo root beyond `.planning/` and `.cursor/`.

## Closest references

- Follow structure locked in `01-CONTEXT.md` (D-01 … D-12) and ADR `.planning/ingest-sources/stack-intent.md`.

## Integration points

- Future phases attach to `apps/api` (Nest modules) and `apps/web-*` (Next routes calling API).

---

## PATTERN MAPPING COMPLETE

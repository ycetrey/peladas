---
phase: 01-funda-o-docker-prisma-e-api-shell
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations
  - apps/api/prisma/seed.ts
  - apps/api/package.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - USR-01
user_setup: []
must_haves:
  truths:
    - "prisma migrate deploy succeeds against empty Postgres"
    - "seed creates at least one User with name, preferredPositions, isAdmin"
  artifacts:
    - apps/api/prisma/schema.prisma
    - apps/api/prisma/migrations/*/migration.sql
    - apps/api/prisma/seed.ts
  key_links:
    - "DATABASE_URL in compose points to db:5432"
---

<objective>
Implementar **Prisma** em `apps/api` com modelo inicial alinhado a `.planning/intel/constraints.md` (User, Match, Registration, Team, TeamPlayer + enums), **migrations** aplicáveis em BD vazio, e **seed** para USR-01 (D-07).

Purpose: Cumprir DATA-01, DATA-02, USR-01 antes do Nest health (plano 03).

Output: `schema.prisma`, primeira migration SQL, `seed.ts`, wiring mínimo PrismaModule exportado.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/intel/constraints.md
@peladas_business_rules_base.md
@.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema and first migration</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations, apps/api/package.json</files>
  <read_first>
    - `.planning/intel/constraints.md` (C-SPEC-001 a C-SPEC-003)
    - `apps/api/package.json`
  </read_first>
  <action>
    Adicionar dependências `prisma`, `@prisma/client` em `apps/api/package.json` com scripts: `"prisma": "prisma"`, `"db:migrate": "prisma migrate deploy"`, `"db:seed": "prisma db seed"`, `"db:generate": "prisma generate"`.
    Em `apps/api/prisma/schema.prisma`: datasource `postgresql` com `url = env("DATABASE_URL")`; generator `client`.
    Modelos Prisma: `User` (id uuid default uuid(), name String, preferredPositions Json ou array enum conforme Prisma — usar `PlayerPosition` enum Prisma), `isAdmin` Boolean, timestamps.
    `Match`, `Registration`, `Team`, `TeamPlayer` com campos alinhados a C-SPEC-003 (tipos DateTime, relações FK).
    Enums: `MatchMode`, `MatchStatus`, `RegistrationStatus`, `PlayerPosition` com valores iguais ao SPEC (strings em DB).
    Executar `pnpm --filter @peladas/api exec prisma migrate dev --name init_domain` **no dev** para gerar pasta `prisma/migrations/.../migration.sql` (commitar SQL).
  </action>
  <acceptance_criteria>
    - `apps/api/prisma/schema.prisma` contém `model User` e `model Match`
    - Existe pelo menos um ficheiro `apps/api/prisma/migrations/*/migration.sql`
    - Enums incluem `enum MatchStatus`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec prisma validate</automated>
  </verify>
  <done>Schema e migration inicial versionados.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migrations to live DB</name>
  <files>apps/api/package.json</files>
  <read_first>
    - `docker-compose.yml`
    - `apps/api/prisma/schema.prisma`
  </read_first>
  <action>
    Com serviço `db` a correr (`docker compose up -d db`), definir `DATABASE_URL` coerente com compose e executar `pnpm --filter @peladas/api exec prisma migrate deploy` (não-interativo). Se destrutivo for inevitável em ambiente de reset, documentar `prisma migrate reset` apenas para dev local — preferir `deploy` em CI-like flow.
  </action>
  <acceptance_criteria>
    - Comando `prisma migrate deploy` termina exit code 0 com DB vazio após migração
    - Output ou logs referem aplicação de migration `init` ou nome equivalente
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec prisma migrate deploy</automated>
  </verify>
  <done>DATA-02 satisfeito contra Postgres real do compose.</done>
</task>

<task type="auto">
  <name>Task 3: prisma db seed for USR-01</name>
  <files>apps/api/prisma/seed.ts, apps/api/package.json</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (USR-01)
    - `apps/api/prisma/schema.prisma`
  </read_first>
  <action>
    Configurar `prisma.seed` em `package.json` do api apontando para `prisma/seed.ts` (node/ts-node ou `tsx` — escolher uma e fixar dependência).
    `seed.ts`: upsert ou create de utilizador exemplo `name: "Seed Jogador"`, `preferredPositions` array com pelo menos `MIDFIELDER`, `isAdmin: false` (e opcionalmente um admin `isAdmin: true`).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @peladas/api exec prisma db seed` exit 0
    - `package.json` de api contém `"prisma":` e referência a seed
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec prisma db seed</automated>
  </verify>
  <done>USR-01 demonstrado via seed.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Seed script → DB | Escreve dados de exemplo; não expor endpoint público sem auth (fora deste plano) |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | T | migration SQL | mitigate | Review PR; usar Prisma migrate, não SQL manual solto |
| T-01-11 | I | seed data | accept | Dados fictícios apenas; sem PII real |
| T-01-12 | E | DATABASE_URL leak | mitigate | `.gitignore` + `.env.example` sem credenciais reais |
</threat_model>

<verification>
- `pnpm --filter @peladas/api exec prisma validate`
- `pnpm --filter @peladas/api exec prisma migrate deploy` com DB up
- `pnpm --filter @peladas/api exec prisma db seed`
</verification>

<success_criteria>
Schema matches domain enums; migrations apply; seed inserts User per USR-01.
</success_criteria>

<output>
Após conclusão: `.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-02-SUMMARY.md`
</output>

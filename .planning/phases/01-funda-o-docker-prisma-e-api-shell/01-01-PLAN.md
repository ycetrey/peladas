---
phase: 01-funda-o-docker-prisma-e-api-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .gitignore
  - docker-compose.yml
  - .env.example
  - README.md
  - apps/api/package.json
  - apps/web-jogador/package.json
  - apps/web-admin/package.json
  - apps/api/Dockerfile
  - apps/web-jogador/Dockerfile
  - apps/web-admin/Dockerfile
autonomous: true
requirements:
  - INFRA-01
user_setup: []
must_haves:
  truths:
    - "README documents single compose command (e.g. docker compose up --build)"
    - "docker compose config validates without error"
  artifacts:
    - docker-compose.yml
    - pnpm-workspace.yaml
    - apps/api/package.json
    - apps/web-jogador/package.json
    - apps/web-admin/package.json
  key_links:
    - "Compose service db uses postgres:16-alpine (or equivalent pinned) with healthcheck"
---

<objective>
Criar a **estrutura monorepo** (pnpm workspaces) com três apps vazios e **docker-compose** na raiz que sobe **db + api + web-jogador + web-admin** com redes e volumes alinhados a `01-CONTEXT.md` (D-01, D-02, D-06, D-09).

Purpose: Desbloquear builds paralelos e o comando único documentado (critério de sucesso da fase 1).

Output: Ficheiros de raiz + skeleton `apps/*` + Compose funcional para `config`/`build` (imagens podem ainda falhar até planos 03–04 — neste plano o foco é **Compose + pastas + documentação**).
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-CONTEXT.md
@.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root workspace and app shells</name>
  <files>package.json, pnpm-workspace.yaml, .gitignore, apps/api/package.json, apps/web-jogador/package.json, apps/web-admin/package.json</files>
  <read_first>
    - `.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-CONTEXT.md` (D-01, D-03)
    - `.planning/REQUIREMENTS.md` (INFRA-01)
  </read_first>
  <action>
    Criar `package.json` na raiz com `"private": true`, `"packageManager": "pnpm@9.15.0"` (ou versão 9.x estável à escolha, documentada), scripts `dev`/`build` placeholders se necessário.
    Criar `pnpm-workspace.yaml` com `packages: ['apps/*']`.
    Criar `apps/api`, `apps/web-jogador`, `apps/web-admin` cada um com `package.json` mínimo (`name`: `@peladas/api`, `@peladas/web-jogador`, `@peladas/web-admin`) e dependências mínimas para mais tarde (Nest/Next podem ser adicionados nos planos seguintes — aqui só nomes e versões compatíveis com Node 22).
    `.gitignore`: `node_modules`, `.env`, `dist`, `.next`, `coverage`.
  </action>
  <acceptance_criteria>
    - Ficheiro `pnpm-workspace.yaml` existe e contém literalmente `apps/`
    - Existem os três caminhos `apps/api/package.json`, `apps/web-jogador/package.json`, `apps/web-admin/package.json`
    - Raiz `package.json` contém `"packageManager": "pnpm@`
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0: adicionar teste de smoke ou script `node -e` que valida JSONs após plano 01-02</automated>
  </verify>
  <done>Workspace reconhecível pelo pnpm; três apps existem com package.json válido.</done>
</task>

<task type="auto">
  <name>Task 2: docker-compose.yml and .env.example</name>
  <files>docker-compose.yml, .env.example</files>
  <read_first>
    - `pnpm-workspace.yaml`
    - `.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-CONTEXT.md` (D-02, D-04, D-06, D-09)
  </read_first>
  <action>
    Criar `docker-compose.yml` na raiz com:
    - Serviço `db`: imagem `postgres:16-alpine`, variável `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB=peladas`, volume `postgres_data`, **healthcheck** `pg_isready -U $$POSTGRES_USER` (sintaxe compose correcta).
    - Rede `peladas-internal` (driver bridge).
    - Serviços `api`, `web-jogador`, `web-admin` com `build.context` apontando para cada `apps/*`, `depends_on` db com `condition: service_healthy` onde aplicável, **volumes** nomeados para `node_modules` em cada app (ex.: `api_node_modules:/app/node_modules`) e bind mount `.:/workspace` ou contexto equivalente documentado em README (per D-09).
    Expor portas: documentar placeholders `API_PORT=3001`, `WEB_JOGADOR_PORT=3002`, `WEB_ADMIN_PORT=3003`, `POSTGRES_PORT=5432` em `.env.example` — não commitar segredos reais.
    Criar **Dockerfile mínimo** em cada `apps/api`, `apps/web-jogador`, `apps/web-admin`: `FROM node:22-bookworm-slim`, `WORKDIR /app`, `COPY package.json ./`, comando `CMD` placeholder (`sleep infinity` ou `node -e` noop) para `docker compose build` não falhar por ficheiro em falta — planos 03–04 substituem por imagens reais Nest/Next.
  </action>
  <acceptance_criteria>
    - `docker-compose.yml` contém o serviço `db` e a string `postgres:16`
    - `docker-compose.yml` define os serviços `api`, `web-jogador`, `web-admin`
    - `.env.example` contém `DATABASE_URL=` e `API_PORT=`
    - Existem `apps/api/Dockerfile`, `apps/web-jogador/Dockerfile`, `apps/web-admin/Dockerfile` cada um com `FROM node:22-bookworm-slim`
  </acceptance_criteria>
  <verify>
    <automated>docker compose config</automated>
  </verify>
  <done>Compose válido; variáveis documentadas.</done>
</task>

<task type="auto">
  <name>Task 3: README — single command</name>
  <files>README.md</files>
  <read_first>
    - `docker-compose.yml`
  </read_first>
  <action>
    Na raiz `README.md`, secção **Quick start** com pré-requisitos (Docker Desktop / Engine + opcionalmente pnpm no host) e comando único: `docker compose up --build` (ou `docker compose --profile dev up` se usar profiles — documentar explicitamente).
    Listar URLs locais esperadas para API e dois frontends após subida.
  </action>
  <acceptance_criteria>
    - `README.md` contém a string `docker compose`
    - `README.md` menciona `web-jogador` e `web-admin` (ou portas documentadas)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "docker compose" README.md</automated>
  </verify>
  <done>INFRA-01 documentação de arranque único satisfeita ao nível de repo.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Host → Compose | Utilizador corre comandos compose; ficheiros `.env` locais não devem ir para git |
| DB ↔ API | `DATABASE_URL` só em env/compose, não hardcoded em imagens públicas |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I | `.env.example` | mitigate | Apenas placeholders; README avisa contra commit de secrets |
| T-01-02 | E | Postgres exposto | mitigate | Porta 5432 mapeada só para dev; documentar remoção em prod |
| T-01-03 | T | docker-compose.yml | mitigate | Revisão em PR; imagens oficiais pinadas |
</threat_model>

<verification>
- `docker compose config` — exit 0
- `grep` INFRA-01 coverage: compose + README presentes
</verification>

<success_criteria>
Compose file exists; three app directories; README states single documented `docker compose` command.
</success_criteria>

<output>
Após conclusão: `.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-01-SUMMARY.md`
</output>

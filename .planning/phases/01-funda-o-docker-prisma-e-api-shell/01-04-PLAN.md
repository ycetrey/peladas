---
phase: 01-funda-o-docker-prisma-e-api-shell
plan: 04
type: execute
wave: 3
depends_on:
  - "01-01"
files_modified:
  - apps/web-jogador/Dockerfile
  - apps/web-admin/Dockerfile
  - apps/web-jogador/next.config.ts
  - apps/web-admin/next.config.ts
  - apps/web-jogador/app/page.tsx
  - apps/web-admin/app/page.tsx
  - apps/web-jogador/package.json
  - apps/web-admin/package.json
  - docker-compose.yml
autonomous: true
requirements:
  - INFRA-03
user_setup: []
must_haves:
  truths:
    - "docker compose build succeeds for web-jogador and web-admin services"
    - "Each Next app serves default page over mapped port in compose"
  artifacts:
    - apps/web-jogador/Dockerfile
    - apps/web-admin/Dockerfile
  key_links:
    - "Compose maps WEB_JOGADOR_PORT and WEB_ADMIN_PORT to 3000 inside containers"
---

<objective>
Substituir Dockerfiles stub dos dois **Next.js** por imagens de desenvolvimento com `next dev` (ou build+start conforme documentado), páginas mínimas, e **compose** atualizado para builds reprodutíveis (INFRA-03, D-11).

Purpose: Critério de sucesso “build das imagens Next completa sem erro” e arranque conjunto com API/DB.

Output: `apps/web-jogador` e `apps/web-admin` com Next 15.x (ou 14 LTS) configurados, Dockerfiles com pnpm install + dev server, portas expostas.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-CONTEXT.md
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js app jogador</name>
  <files>apps/web-jogador/package.json, apps/web-jogador/app/page.tsx, apps/web-jogador/next.config.ts, apps/web-jogador/tsconfig.json</files>
  <read_first>
    - `apps/web-jogador/package.json`
  </read_first>
  <action>
    Adicionar dependências `next`, `react`, `react-dom` com versões alinhadas; `scripts`: `"dev": "next dev --hostname 0.0.0.0"`, `"build": "next build"`.
    Criar `app/page.tsx` minimal (título "Peladas — Jogador").
    `next.config.ts` ou `.mjs` vazio export default `nextConfig`.
  </action>
  <acceptance_criteria>
    - `apps/web-jogador/package.json` contém `"next":`
    - `apps/web-jogador/app/page.tsx` existe
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-jogador exec next build</automated>
  </verify>
  <done>App jogador compila.</done>
</task>

<task type="auto">
  <name>Task 2: Next.js app admin</name>
  <files>apps/web-admin/package.json, apps/web-admin/app/page.tsx, apps/web-admin/next.config.ts, apps/web-admin/tsconfig.json</files>
  <read_first>
    - `apps/web-jogador/package.json`
  </read_first>
  <action>
    Espelhar estrutura do jogador com nome `@peladas/web-admin` e página "Peladas — Admin".
  </action>
  <acceptance_criteria>
    - `apps/web-admin/package.json` contém `"next":`
    - `apps/web-admin/app/page.tsx` contém `Admin`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-admin exec next build</automated>
  </verify>
  <done>App admin compila.</done>
</task>

<task type="auto">
  <name>Task 3: Dockerfiles + compose for both fronts</name>
  <files>apps/web-jogador/Dockerfile, apps/web-admin/Dockerfile, docker-compose.yml</files>
  <read_first>
    - `docker-compose.yml`
    - `.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-CONTEXT.md` (D-09)
  </read_first>
  <action>
    Dockerfiles: base `node:22-bookworm-slim`, `corepack`+pnpm, `COPY` package files, `pnpm install`, CMD `pnpm dev` (watch) com `WORKDIR` correto; expor `3000`.
    Atualizar compose: `web-jogador` e `web-admin` com `ports` mapeando `${WEB_JOGADOR_PORT:-3002}:3000` e `${WEB_ADMIN_PORT:-3003}:3000`, volumes `node_modules` nomeados + bind source conforme D-09.
  </action>
  <acceptance_criteria>
    - `docker compose build web-jogador web-admin` exit 0
    - Ambos Dockerfiles contêm `next` ou `pnpm dev`
  </acceptance_criteria>
  <verify>
    <automated>docker compose build web-jogador web-admin</automated>
  </verify>
  <done>INFRA-03 satisfeito.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next dev | Dev server não endurecido — aceitável só em dev local |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-30 | S | dev server | accept | Apenas rede local compose; não exposição pública |
| T-01-31 | E | outdated deps | mitigate | Pin major versions em package.json |
</threat_model>

<verification>
- `docker compose build web-jogador web-admin`
- Optional: `curl` páginas HTML após `docker compose up`
</verification>

<success_criteria>
Both Next images build; compose documents ports for jogador and admin.
</success_criteria>

<output>
Após conclusão: `.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-04-SUMMARY.md`
</output>

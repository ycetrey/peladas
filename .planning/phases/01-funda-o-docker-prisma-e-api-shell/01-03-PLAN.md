---
phase: 01-funda-o-docker-prisma-e-api-shell
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - apps/api/Dockerfile
  - apps/api/nest-cli.json
  - apps/api/tsconfig.json
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/health/health.controller.ts
  - apps/api/src/health/health.module.ts
  - apps/api/src/prisma/prisma.service.ts
  - apps/api/src/prisma/prisma.module.ts
  - docker-compose.yml
autonomous: true
requirements:
  - INFRA-02
user_setup: []
must_haves:
  truths:
    - "GET /health returns HTTP 200 with JSON body containing status ok inside running api container"
    - "API Docker image builds successfully"
  artifacts:
    - apps/api/Dockerfile
    - apps/api/src/health/health.controller.ts
  key_links:
    - "Compose service api uses Dockerfile that runs prisma generate && nest start"
---

<objective>
Substituir o **Dockerfile stub** da API por imagem **NestJS** funcional com **Prisma Client** gerado, **`GET /health`**, e comando de arranque compatível com Compose (per D-10, D-11, INFRA-02).

Purpose: Critério de sucesso “API em container responde health/ping”.

Output: Nest app mínima, módulos Health + Prisma, Dockerfile multi-stage ou single com `pnpm install`, `prisma generate`, `nest start --watch` em dev.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-CONTEXT.md
@.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-RESEARCH.md
@apps/api/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: NestJS scaffold in apps/api</name>
  <files>apps/api/src/main.ts, apps/api/src/app.module.ts, apps/api/nest-cli.json, apps/api/tsconfig.json, apps/api/package.json</files>
  <read_first>
    - `apps/api/package.json`
    - `apps/api/prisma/schema.prisma`
  </read_first>
  <action>
    Instalar dependências Nest: `@nestjs/core`, `@nestjs/common`, `@nestjs/platform-express`, `reflect-metadata`, `rxjs`, `@nestjs/config` (opcional), devDependencies `@nestjs/cli`, `typescript`, `ts-node`.
    Criar `src/main.ts` com `NestFactory.create(AppModule)` e `listen(process.env.PORT ?? 3001)`.
    `AppModule` importa `HealthModule` e `PrismaModule`.
    `nest-cli.json` e `tsconfig.json` padrão Nest.
  </action>
  <acceptance_criteria>
    - `apps/api/src/main.ts` contém `NestFactory`
    - `apps/api/package.json` contém `@nestjs/core`
    - `apps/api/src/app.module.ts` existe
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec nest build</automated>
  </verify>
  <done>API compila localmente.</done>
</task>

<task type="auto">
  <name>Task 2: Health module</name>
  <files>apps/api/src/health/health.controller.ts, apps/api/src/health/health.module.ts</files>
  <read_first>
    - `apps/api/src/app.module.ts`
  </read_first>
  <action>
    `HealthController` com `@Get('health')` retornando `{ "status": "ok", "service": "peladas-api" }` e código HTTP 200.
    Registar `HealthModule` em `AppModule`.
  </action>
  <acceptance_criteria>
    - `health.controller.ts` contém o path `health` e a string `ok`
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0: e2e supertest GET /health</automated>
  </verify>
  <done>Endpoint de health disponível.</done>
</task>

<task type="auto">
  <name>Task 3: PrismaModule + Dockerfile API</name>
  <files>apps/api/src/prisma/prisma.service.ts, apps/api/src/prisma/prisma.module.ts, apps/api/Dockerfile, docker-compose.yml</files>
  <read_first>
    - `docker-compose.yml`
    - `apps/api/prisma/schema.prisma`
  </read_first>
  <action>
    `PrismaService` extends `PrismaClient` com `onModuleInit` `$connect()` e `onModuleDestroy` `$disconnect()`.
    `PrismaModule` `@Global()` exporta `PrismaService`.
    **Dockerfile** API: estágio base `node:22-bookworm-slim`, instalar `corepack enable && corepack prepare pnpm@9.15.0 --activate`, `COPY` workspace ou apenas `apps/api` conforme estratégia escolhida (documentar se monorepo full copy ou só api); comandos: `pnpm install`, `pnpm exec prisma generate`, `pnpm exec nest build`, `CMD` para `node dist/main.js` (prod) ou `pnpm exec nest start --watch` (dev) — alinhar com variável `NODE_ENV` no compose.
    Atualizar `docker-compose.yml` serviço `api`: `build.dockerfile` se necessário, env `DATABASE_URL`, `PORT`, `depends_on` db healthy.
  </action>
  <acceptance_criteria>
    - `Dockerfile` na pasta `apps/api` contém `prisma generate`
    - `prisma.service.ts` contém `PrismaClient`
    - `docker compose build api` completa sem erro (após planos anteriores aplicados)
  </acceptance_criteria>
  <verify>
    <automated>docker compose build api</automated>
  </verify>
  <done>INFRA-02: imagem API reprodutível com health.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /health | Endpoint público só expõe estado; sem dados sensíveis |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20 | D | /health | accept | Sem dados persistentes; rate-limit fora do escopo fase 1 |
| T-01-21 | E | Nest default | mitigate | Não expor stack traces em prod (`NODE_ENV=production`) |
| T-01-22 | I | Headers | mitigate | Helmet opcional — marcar como melhoria pós-fase-1 se não couber |
</threat_model>

<verification>
- `docker compose up -d db api` then `curl -sf http://127.0.0.1:${API_PORT:-3001}/health`
</verification>

<success_criteria>
API container builds; /health returns 200 JSON.
</success_criteria>

<output>
Após conclusão: `.planning/phases/01-funda-o-docker-prisma-e-api-shell/01-03-SUMMARY.md`
</output>

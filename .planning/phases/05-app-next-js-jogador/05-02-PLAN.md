---
phase: 05-app-next-js-jogador
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - apps/api/src/registrations/registrations.controller.ts
  - apps/api/src/registrations/registrations.service.ts
  - apps/api/src/registrations/registrations.e2e-spec.ts
  - README.md
  - apps/web-jogador/app/matches/[id]/page.tsx
  - apps/web-jogador/lib/api.ts
  - apps/web-jogador/README.md
autonomous: true
requirements:
  - UIJ-02
  - UIJ-03
user_setup: []
must_haves:
  truths:
    - "GET /matches/:matchId/registrations/me devolve inscrição ativa ou null"
    - "UI: inscrever e cancelar sem curl; estado titular/reserva/cancelado visível"
  artifacts:
    - "registrations.controller.ts — GET me"
    - "app/matches/[id]/page.tsx — fluxo inscrição"
  key_links:
    - "05-UI-SPEC.md secção detalhe"
---

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-03 | Spoofing | X-Player-User-Id | ACCEPT (v1) | UUID escolhido pelo utilizador — documentar risco; v2 autenticação |
| T-04 | Injection | JSON body | MITIGATE | Manter DTO com `@IsEnum`; não interpretar HTML em mensagens (`textContent` ou React default) |
| T-05 | Information disclosure | GET me | MITIGATE | Só devolver inscrição do utilizador do header; não listar outros |
</threat_model>

<objective>
Adicionar **GET inscrição “me”** na API; completar página de detalhe com **inscrever**, **cancelar** e **estado da inscrição** com labels humanos.

Purpose: Fechar UIJ-02 e UIJ-03.

Output: endpoint GET, métodos em `lib/api.ts`, UI na página `[id]`, README do app jogador.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/api/src/registrations/registrations.controller.ts
@apps/api/src/registrations/registrations.service.ts
@apps/api/src/registrations/player-user.guard.ts
@apps/web-jogador/app/matches/[id]/page.tsx
@.planning/phases/05-app-next-js-jogador/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API — GET registrations/me</name>
  <files>apps/api/src/registrations/registrations.service.ts, apps/api/src/registrations/registrations.controller.ts</files>
  <read_first>
    - `apps/api/src/registrations/registrations.service.ts`
    - `apps/api/src/registrations/registrations.controller.ts`
    - `apps/api/prisma/schema.prisma` (RegistrationStatus)
  </read_first>
  <action>
    `RegistrationsService.findMine(matchId, userId)`: `findFirst` com `matchId`, `userId`, `status in [CONFIRMED,SUBSTITUTE]`; se não existir devolver `{ registration: null }`; se existir `{ registration }` (objeto Prisma serializável).
    `RegistrationsController`: adicionar `@Get('me')` com `PlayerUserGuard`, `@HttpCode(200)`, retornar `findMine`. **Import de valor** `RegisterPlayerDto` já corrigido em ficheiros existentes — não repetir `import type` para DTOs com `@Body()`.
    Se `Match` não existir: usar `MatchNotFoundError` (404) antes de query — carregar match por id.
  </action>
  <acceptance_criteria>
    - `registrations.controller.ts` contém `@Get('me')`
    - `registrations.service.ts` contém método nomeado `findMine` ou equivalente grepável
  </acceptance_criteria>
  <verify>
    <automated>npx nest build em apps/api</automated>
  </verify>
  <done>Leitura de estado para UI.</done>
</task>

<task type="auto">
  <name>Task 2: API — e2e GET me + README</name>
  <files>apps/api/src/registrations/registrations.e2e-spec.ts, README.md</files>
  <read_first>
    - `apps/api/src/registrations/registrations.e2e-spec.ts`
    - `README.md` secção Inscrições
  </read_first>
  <action>
    Em e2e existente ou novo `it`, após criar match e um `POST` registo, `GET .../registrations/me` com `X-Player-User-Id` espera `200` e `body.registration.status` definido; caso sem registo, `registration` null.
    README: documentar `GET http://localhost:3001/matches/<id>/registrations/me` com cabeçalho jogador.
  </action>
  <acceptance_criteria>
    - `registrations.e2e-spec.ts` contém `get(` e `registrations/me`
    - `README.md` contém `registrations/me`
  </acceptance_criteria>
  <verify>
    <automated>npx jest --config jest-e2e.config.cjs --runInBand src/registrations/registrations.e2e-spec.ts</automated>
  </verify>
  <done>Contrato documentado e testado.</done>
</task>

<task type="auto">
  <name>Task 3: web-jogador — lib api register/cancel/getMine</name>
  <files>apps/web-jogador/lib/api.ts</files>
  <read_first>
    - `apps/web-jogador/lib/api.ts`
  </read_first>
  <action>
    Adicionar `getMyRegistration(matchId, playerUserId)` → GET com header `X-Player-User-Id`.
    `registerForMatch(matchId, playerUserId, preferredPosition)` POST JSON.
    `cancelMyRegistration(matchId, playerUserId)` DELETE sem corpo.
    Reutilizar tratamento de erro existente.
  </action>
  <acceptance_criteria>
    - `lib/api.ts` exporta as três funções (grep por nomes)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-jogador run build</automated>
  </verify>
  <done>Chamadas mutação prontas.</done>
</task>

<task type="auto">
  <name>Task 4: UI — secção inscrição no detalhe</name>
  <files>apps/web-jogador/app/matches/[id]/page.tsx</files>
  <read_first>
    - `apps/web-jogador/app/matches/[id]/page.tsx`
    - `05-UI-SPEC.md`
  </read_first>
  <action>
    Ler `localStorage.getItem('peladas_player_user_id')`; se vazio, mostrar aviso para preencher no topo.
    Ao montar e após ações: chamar `getMyRegistration` e mostrar **Titular** (`CONFIRMED`), **Reserva** (`SUBSTITUTE`), **Cancelado** (`CANCELED` se API alguma vez devolver histórico — se só ativos, “Sem inscrição ativa” quando `null`).
    Select `PlayerPosition` + botão “Inscrever” chama `registerForMatch` com UUID guardado.
    Botão “Cancelar inscrição” com `window.confirm`, chama `cancelMyRegistration`, depois refetch estado e detalhe da partida se necessário.
    Mapear erros da API (`error.code`) para mensagem visível (`RegistrationClosedError`, etc.).
  </action>
  <acceptance_criteria>
    - `page.tsx` contém string `getMyRegistration` ou import equivalente
    - `page.tsx` contém `registerForMatch` e `cancelMyRegistration`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-jogador run build</automated>
  </verify>
  <done>UIJ-02 e UIJ-03 na UI.</done>
</task>

<task type="auto">
  <name>Task 5: apps/web-jogador/README.md</name>
  <files>apps/web-jogador/README.md</files>
  <read_first>
    - `apps/web-jogador/package.json`
  </read_first>
  <action>
    Criar README com: como correr `pnpm --filter @peladas/web-jogador run dev`, dependência da API, variável `NEXT_PUBLIC_API_BASE_URL`, explicação do painel de UUID, fluxo UAT em 5 passos.
  </action>
  <acceptance_criteria>
    - `apps/web-jogador/README.md` existe e contém `NEXT_PUBLIC_API_BASE_URL`
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('fs').accessSync('apps/web-jogador/README.md')"</automated>
  </verify>
  <done>Onboarding jogador.</done>
</task>

</tasks>

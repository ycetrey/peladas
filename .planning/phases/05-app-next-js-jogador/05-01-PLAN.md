---
phase: 05-app-next-js-jogador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/main.ts
  - docker-compose.yml
  - apps/web-jogador/package.json
  - apps/web-jogador/app/layout.tsx
  - apps/web-jogador/app/page.tsx
  - apps/web-jogador/lib/api.ts
  - apps/web-jogador/lib/types.ts
autonomous: true
requirements:
  - UIJ-01
user_setup: []
must_haves:
  truths:
    - "Lista de partidas visível em / com dados da API"
    - "Página /matches/[id] mostra detalhe mínimo da partida (GET /matches/:id)"
    - "Browser consegue chamar API (CORS + NEXT_PUBLIC_API_BASE_URL no compose)"
  artifacts:
    - apps/web-jogador/app/page.tsx
    - apps/web-jogador/lib/api.ts
  key_links:
    - "05-UI-SPEC.md layout e lista"
    - "05-CONTEXT.md decisão CORS"
---

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-01 | Tampering | CORS | MITIGATE | `enableCors` com lista explícita de origens (localhost:3002 + env); não `*` em produção futura |
| T-02 | Information disclosure | `NEXT_PUBLIC_*` | ACCEPT | URL da API pública ao browser é esperada; sem segredos em `NEXT_PUBLIC` |
</threat_model>

<objective>
Entregar **lista + detalhe soma leitura** no `web-jogador`, **cliente HTTP partilhado**, **CORS na API**, e **env Docker** para a app saber onde está a API.

Purpose: Fechar leitura UIJ-01 (lista + abrir detalhe).

Output: `lib/api.ts`, `lib/types.ts`, `/` e `/matches/[id]`, `main.ts` CORS, compose env.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-app-next-js-jogador/05-CONTEXT.md
@.planning/phases/05-app-next-js-jogador/05-UI-SPEC.md
@README.md
@docker-compose.yml
@apps/api/src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: API — enableCors para origem do web-jogador</name>
  <files>apps/api/src/main.ts</files>
  <read_first>
    - `apps/api/src/main.ts`
  </read_first>
  <action>
    Após `createNestApplication`, chamar `app.enableCors({ origin: (process.env.WEB_JOGADOR_ORIGIN ?? 'http://localhost:3002').split(',').map(s => s.trim()).filter(Boolean), methods: ['GET','POST','DELETE','OPTIONS'], allowedHeaders: ['Content-Type','X-Organizer-User-Id','X-Player-User-Id'] })`.
    Documentar `WEB_JOGADOR_ORIGIN` em `README.md` (secção API ou Compose).
  </action>
  <acceptance_criteria>
    - `main.ts` contém `enableCors`
    - `README.md` menciona `WEB_JOGADOR_ORIGIN` ou origem padrão `http://localhost:3002`
  </acceptance_criteria>
  <verify>
    <automated>npx nest build em apps/api</automated>
  </verify>
  <done>CORS preparado.</done>
</task>

<task type="auto">
  <name>Task 2: Compose — NEXT_PUBLIC_API_BASE_URL no web-jogador</name>
  <files>docker-compose.yml</files>
  <read_first>
    - `docker-compose.yml`
  </read_first>
  <action>
    No serviço `web-jogador.environment`, adicionar `NEXT_PUBLIC_API_BASE_URL: http://localhost:3001` (ou `${API_PUBLIC_URL:-http://localhost:3001}` se preferires variável de raiz `.env`).
    Garantir que o comentário no README explica: browser no host usa `localhost:3001` para alcançar a API exposta pelo compose.
  </action>
  <acceptance_criteria>
    - `docker-compose.yml` contém `NEXT_PUBLIC_API_BASE_URL`
  </acceptance_criteria>
  <verify>
    <automated>grep NEXT_PUBLIC_API_BASE_URL docker-compose.yml</automated>
  </verify>
  <done>Env público definido.</done>
</task>

<task type="auto">
  <name>Task 3: lib/types + lib/api fetch JSON</name>
  <files>apps/web-jogador/lib/types.ts, apps/web-jogador/lib/api.ts</files>
  <read_first>
    - `apps/api/src/matches/matches.service.ts` (formato `{ matches }`, `{ match }`)
  </read_first>
  <action>
    Criar `lib/types.ts` com interfaces mínimas `Match`, `MatchListResponse`, `MatchDetailResponse` alinhadas ao JSON real (campos string de enums como literais union TypeScript).
    Criar `lib/api.ts` com `getApiBaseUrl(): string` lendo `process.env.NEXT_PUBLIC_API_BASE_URL` com fallback `http://localhost:3001`, função `apiFetch<T>(path, init?)` que prefixa base URL, `Content-Type: application/json` em POST, e em erro `!response.ok` lê JSON `{ error?: { code, message } }` e lança `Error` com `message` legível.
    Exportar `listMatches()`, `getMatch(id: string)`.
  </action>
  <acceptance_criteria>
    - `apps/web-jogador/lib/api.ts` existe e exporta `listMatches` e `getMatch`
    - `listMatches` usa caminho `/matches`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-jogador run build</automated>
  </verify>
  <done>Cliente tipado.</done>
</task>

<task type="auto">
  <name>Task 4: Layout — painel “O teu ID” (localStorage)</name>
  <files>apps/web-jogador/app/layout.tsx, apps/web-jogador/app/components/player-id-settings.tsx</files>
  <read_first>
    - `apps/web-jogador/app/layout.tsx`
    - `.planning/phases/05-app-next-js-jogador/05-UI-SPEC.md`
  </read_first>
  <action>
    Componente cliente `'use client'` `PlayerIdSettings` com input texto, botão Guardar, chave `peladas_player_user_id`, validação regex UUID igual à API (copiar padrão de `player-user.guard.ts` se necessário), mensagem de erro se inválido.
    Inserir no `layout.tsx` dentro de `<body>` acima de `{children}`.
  </action>
  <acceptance_criteria>
    - Ficheiro `player-id-settings.tsx` com `'use client'`
    - `layout.tsx` renderiza `PlayerIdSettings`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-jogador run build</automated>
  </verify>
  <done>Identidade local pronta.</done>
</task>

<task type="auto">
  <name>Task 5: Página inicial — lista de partidas</name>
  <files>apps/web-jogador/app/page.tsx</files>
  <read_first>
    - `apps/web-jogador/lib/api.ts`
    - `05-UI-SPEC.md`
  </read_first>
  <action>
    Server ou Client: usar **Client Component** com `useEffect`+`useState` que chama `listMatches()` para alinhar com env público sem configurar `API_URL` só-servidor.
    Mostrar loading, erro, lista com link `next/link` para `/matches/${id}`; exibir `title`, `dateTime` localizado `pt-PT`, `status`, `mode`.
  </action>
  <acceptance_criteria>
    - `page.tsx` importa `listMatches` de `lib/api` ou re-export
    - `page.tsx` contém `href={`/matches/` (padrão grep)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-jogador run build</automated>
  </verify>
  <done>UIJ-01 lista OK.</done>
</task>

<task type="auto">
  <name>Task 6: Página detalhe — leitura</name>
  <files>apps/web-jogador/app/matches/[id]/page.tsx</files>
  <read_first>
    - `apps/web-jogador/lib/api.ts`
  </read_first>
  <action>
    Criar `app/matches/[id]/page.tsx` como Client Component: ao montar, `getMatch(params.id)`; mostrar título, datas de inscrição, `maxPlayers`, `mode`, `status`; link Voltar para `/`. Secção “Inscrição” pode ficar placeholder até 05-03.
  </action>
  <acceptance_criteria>
    - Ficheiro `apps/web-jogador/app/matches/[id]/page.tsx` existe
    - Contém chamada a `getMatch`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-jogador run build</automated>
  </verify>
  <done>UIJ-01 detalhe leitura OK.</done>
</task>

</tasks>

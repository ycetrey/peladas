# Phase 5 — Pattern map

| New / touched | Closest analog in repo | Notes |
|---------------|-------------------------|--------|
| `apps/web-jogador/lib/api.ts` | — (greenfield) | Thin `fetch` wrapper + `throw` on !ok |
| `apps/web-jogador/app/page.tsx` | `apps/web-jogador/app/layout.tsx` | Replace placeholder home |
| Player settings UI | — | Small client island in layout or header |
| `GET .../registrations/me` | `DELETE .../registrations/me` same controller | Mirror guard + param |

---

*Phase: 05-app-next-js-jogador*

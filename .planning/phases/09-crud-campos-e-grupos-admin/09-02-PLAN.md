---
phase: 09-crud-campos-e-grupos-admin
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01-PLAN.md
files_modified:
  - apps/web-admin/lib/api.ts
  - apps/web-admin/lib/types.ts
  - apps/web-admin/app/(app)/venues/page.tsx
  - apps/web-admin/app/(app)/venues/nova/page.tsx
  - apps/web-admin/app/(app)/venues/[id]/editar/page.tsx
  - apps/web-admin/app/(app)/groups/page.tsx
  - apps/web-admin/app/(app)/groups/nova/page.tsx
  - apps/web-admin/app/(app)/groups/[id]/editar/page.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Rotas /venues/nova, /venues/[id]/editar, /groups/nova, /groups/[id]/editar existem e compilam com next build"
    - "lib/api.ts expõe getVenue, updateVenue, deleteVenue, getGroup, updateGroup, deleteGroup usando backendPath e credentials include"
    - "Listagens /venues e /groups usam panel-card e links para editar como no painel de partidas"
  artifacts:
    - "apps/web-admin/app/(app)/venues/[id]/editar/page.tsx"
    - "apps/web-admin/app/(app)/groups/[id]/editar/page.tsx"
  key_links:
    - "09-UI-SPEC.md"
    - "09-01-PLAN.md (API deve estar merged antes desta wave)"
---

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-09-03 | CSRF | cookies session | MITIGATE | Manter mesmo padrão que partidas: fetch com credentials para /api/backend |
| T-09-04 | XSS | mensagens API | MITIGATE | Renderizar erro como texto; sem dangerouslySetInnerHTML |
</threat_model>

<objective>
Reestruturar **Campos** e **Grupos** no admin para o mesmo modelo de **listagem + nova + editar** das partidas, consumindo a API da wave 1 (`get/patch/delete` por id).
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-crud-campos-e-grupos-admin/09-UI-SPEC.md
@.planning/phases/09-crud-campos-e-grupos-admin/09-CONTEXT.md
@apps/web-admin/app/(app)/page.tsx
@apps/web-admin/app/(app)/partidas/[id]/editar/page.tsx
@apps/web-admin/app/(app)/venues/page.tsx
@apps/web-admin/app/(app)/groups/page.tsx
@apps/web-admin/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cliente API admin</name>
  <files>apps/web-admin/lib/api.ts, apps/web-admin/lib/types.ts</files>
  <read_first>apps/web-admin/lib/api.ts, apps/web-admin/lib/types.ts</read_first>
  <action>
    Adicionar funções:
    - `getVenue(id: string): Promise&lt;Venue&gt;` — GET `/venues/${id}` → `data.venue`
    - `updateVenue(id: string, body: { name?: string; locality?: string | null }): Promise&lt;Venue&gt;` — PATCH
    - `deleteVenue(id: string): Promise&lt;void&gt;` — DELETE
    - `getGroup(id: string): Promise&lt;GroupWithMembers&gt;` — GET `/groups/${id}` → `data.group`
    - `updateGroup(id: string, name: string): Promise&lt;GroupWithMembers&gt;` — PATCH
    - `deleteGroup(id: string): Promise&lt;void&gt;` — DELETE
    Usar `apiFetch` com `method: "PATCH"`, `jsonBody` quando aplicável. Se resposta DELETE não tiver corpo, `apiFetch` deve aceitar resposta vazia (já trata texto vazio).
    Tipos: reutilizar `Venue`, `GroupWithMembers`; acrescentar tipos de resposta `{ venue: Venue }`, `{ group: GroupWithMembers }` se necessário em `types.ts`.
  </action>
  <acceptance_criteria>
    - `api.ts` contém as seis funções com `backendPath(\`/venues/\${id}\`)` e `backendPath(\`/groups/\${id}\`)` para get/patch/delete
    - Nenhum `fetch` directo a `getApiBaseUrl()` fora do padrão existente
  </acceptance_criteria>
  <verify>
    <automated>grep -n "getVenue\\|updateVenue\\|deleteVenue\\|getGroup\\|updateGroup\\|deleteGroup" apps/web-admin/lib/api.ts</automated>
  </verify>
  <done>Cliente HTTP pronto.</done>
</task>

<task type="auto">
  <name>Task 2: Venues — páginas list / nova / editar</name>
  <files>apps/web-admin/app/(app)/venues/page.tsx, apps/web-admin/app/(app)/venues/nova/page.tsx, apps/web-admin/app/(app)/venues/[id]/editar/page.tsx</files>
  <read_first>apps/web-admin/app/(app)/venues/page.tsx, apps/web-admin/app/(app)/partidas/nova/page.tsx, apps/web-admin/app/(app)/partidas/[id]/editar/page.tsx</read_first>
  <action>
    **page.tsx (listagem):** Remover formulários de criação; manter lista em `panel-card` com `Link` para `/venues/nova` (botão `btn btn-primary` "Novo campo") e cada linha/cartão com `Link` para `/venues/${v.id}/editar`. Opcional: mostrar localidade e badge Google.
    **nova/page.tsx:** Mover conteúdo actual de criação (Google Places + form manual) deste ficheiro; topo com `Link` className `btn btn-secondary` "← Voltar" para `/venues`; após sucesso `router.push("/venues")` ou `/venues/${id}/editar` (escolher **/venues** para consistência com D-01).
    **editar/page.tsx:** `useParams` para `id`; `useEffect` load `getVenue`; estado para name/locality; barra `row` justify space-between: Voltar → `/venues`, Salvar (PATCH), Deletar (confirm, DELETE, redirect `/venues`); mostrar `alert` se erro; se 409 mostrar mensagem devolvida pela API.
  </action>
  <acceptance_criteria>
    - Existem três ficheiros sob `app/(app)/venues/` com os paths acima
    - `venues/page.tsx` não contém `createVenue(` (criação só em nova/editar)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-admin exec next build</automated>
  </verify>
  <done>Fluxo venues completo na UI.</done>
</task>

<task type="auto">
  <name>Task 3: Groups — páginas list / nova / editar</name>
  <files>apps/web-admin/app/(app)/groups/page.tsx, apps/web-admin/app/(app)/groups/nova/page.tsx, apps/web-admin/app/(app)/groups/[id]/editar/page.tsx</files>
  <read_first>apps/web-admin/app/(app)/groups/page.tsx, apps/web-admin/app/(app)/venues/[id]/editar/page.tsx (após criado)</read_first>
  <action>
    **groups/page.tsx:** Só listagem: nome, número de membros, `Link` para `/groups/nova` e para `/groups/${g.id}/editar`. Remover inline create + member forms desta página.
    **groups/nova/page.tsx:** Form só com nome do grupo; submit `createGroup`; redirect lista.
    **groups/[id]/editar/page.tsx:** Carregar `getGroup`; editar nome (PATCH); secção membros copiada da página actual (add/remove com `addGroupMember` / `removeGroupMember`); Deletar grupo com confirmação e tratamento 409.
    Substituir `border-top: 1px solid #333` por classes existentes (`panel-card` ou `muted`) para consistência visual.
  </action>
  <acceptance_criteria>
    - `groups/page.tsx` contém `Link` para `/groups/nova` e para editar por id
    - `groups/[id]/editar/page.tsx` chama `updateGroup` e `deleteGroup`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-admin exec next build</automated>
  </verify>
  <done>Fluxo groups completo na UI.</done>
</task>

</tasks>

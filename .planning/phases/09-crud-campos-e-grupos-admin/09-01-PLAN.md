---
phase: 09-crud-campos-e-grupos-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/common/errors/domain-errors.ts
  - apps/api/src/common/filters/domain-exception.filter.ts
  - apps/api/src/venues/dto/update-venue.dto.ts
  - apps/api/src/venues/venues.service.ts
  - apps/api/src/venues/venues.controller.ts
  - apps/api/src/groups/dto/update-group.dto.ts
  - apps/api/src/groups/groups.service.ts
  - apps/api/src/groups/groups.controller.ts
  - apps/api/src/venues/venues.e2e-spec.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "GET /venues/:id e GET /groups/:id devolvem 404 VenueNotFoundError/GroupNotFoundError se id inexistente ou não pertencer ao admin"
    - "PATCH e DELETE respeitam dono; DELETE com matches associados devolve 409 com código VenueHasMatchesError ou GroupHasMatchesError"
    - "e2e cobre patch venue, delete venue livre, delete venue bloqueado com match"
  artifacts:
    - "apps/api/src/venues/venues.e2e-spec.ts"
    - "apps/api/src/venues/dto/update-venue.dto.ts"
    - "apps/api/src/groups/dto/update-group.dto.ts"
  key_links:
    - "09-CONTEXT.md D-02–D-06"
---

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-09-01 | IDOR | venues/groups :id | MITIGATE | Todas as operações filtram por createdByUserId + AdminGuard |
| T-09-02 | Data loss | DELETE venue/group | MITIGATE | Bloquear com 409 se existirem matches; mensagem clara; teste e2e |
</threat_model>

<objective>
Completar API CRUD para **venues** e **groups**: leitura por id, atualização (PATCH) e remoção (DELETE) com regras de dono e conflito quando houver partidas. Adicionar erros de domínio e mapeamento HTTP 409. Cobrir com testes e2e Supertest.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-crud-campos-e-grupos-admin/09-CONTEXT.md
@.planning/phases/09-crud-campos-e-grupos-admin/09-RESEARCH.md
@apps/api/src/venues/venues.controller.ts
@apps/api/src/venues/venues.service.ts
@apps/api/src/groups/groups.controller.ts
@apps/api/src/groups/groups.service.ts
@apps/api/src/common/filters/domain-exception.filter.ts
@apps/api/src/matches/matches.e2e-spec.ts
@apps/api/test/e2e-auth.ts
@apps/api/test/e2e-default-venue.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Erros de domínio + filtro 409</name>
  <files>apps/api/src/common/errors/domain-errors.ts, apps/api/src/common/filters/domain-exception.filter.ts</files>
  <read_first>apps/api/src/common/errors/domain-errors.ts, apps/api/src/common/filters/domain-exception.filter.ts</read_first>
  <action>
    Em `domain-errors.ts`, adicionar classes `VenueHasMatchesError` e `GroupHasMatchesError` com `readonly code` igual ao nome da classe e mensagens em português (ex.: "Não é possível apagar este campo porque existem partidas associadas.").
    Em `domain-exception.filter.ts`, definir `conflictCodes` com estes dois códigos e responder **HttpStatus.CONFLICT** (409) com body `{ error: { code, message } }` igual aos outros.
  </action>
  <acceptance_criteria>
    - `domain-errors.ts` contém `VenueHasMatchesError` e `GroupHasMatchesError`
    - `domain-exception.filter.ts` importa `HttpStatus` se necessário e usa status 409 para esses códigos
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && pnpm exec tsc --noEmit -p tsconfig.build.json 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <done>Erros e filtro prontos.</done>
</task>

<task type="auto">
  <name>Task 2: Venues — DTO, serviço, controlador</name>
  <files>apps/api/src/venues/dto/update-venue.dto.ts, apps/api/src/venues/venues.service.ts, apps/api/src/venues/venues.controller.ts</files>
  <read_first>apps/api/src/venues/dto/create-venue.dto.ts, apps/api/src/venues/venues.service.ts, apps/api/src/venues/venues.controller.ts</read_first>
  <action>
    Criar `UpdateVenueDto` com `name?: string`, `locality?: string` usando `class-validator` (`IsOptional`, `IsString`, `MinLength` onde fizer sentido — alinhar a `CreateVenueDto`).
    Em `VenuesService`: `findOne(id, adminUserId)` devolve `{ venue }` ou `VenueNotFoundError`; `update(id, adminUserId, dto)` mesma verificação de dono; `remove(id, adminUserId)` verifica dono, depois `_count` de `matches` onde `venueId === id`, se &gt; 0 lança `VenueHasMatchesError`, senão `prisma.venue.delete`.
    Em `VenuesController`: declarar **depois** de `Get('places/suggest')` e **antes** de qualquer rota ambígua: `Get(':id')`, `Patch(':id')`, `Delete(':id')` com `AdminGuard` + JWT. PATCH retorna `{ venue }`; DELETE ret `{ deleted: true }` ou 204 conforme padrão do projeto (preferir body JSON consistente com outros deletes se existirem).
  </action>
  <acceptance_criteria>
    - `venues.controller.ts` expõe `GET /venues/:id`, `PATCH /venues/:id`, `DELETE /venues/:id` protegidos por admin
    - `venues.service.ts` usa `VenueHasMatchesError` no delete quando `prisma.match.count` &gt; 0
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Patch\\|Delete\\|findOne" apps/api/src/venues/venues.controller.ts</automated>
  </verify>
  <done>Venues CRUD API completo.</done>
</task>

<task type="auto">
  <name>Task 3: Groups — DTO, serviço, controlador</name>
  <files>apps/api/src/groups/dto/update-group.dto.ts, apps/api/src/groups/groups.service.ts, apps/api/src/groups/groups.controller.ts</files>
  <read_first>apps/api/src/groups/dto/create-group.dto.ts, apps/api/src/groups/groups.service.ts, apps/api/src/groups/groups.controller.ts</read_first>
  <action>
    Criar `UpdateGroupDto` com `name?: string` validado.
    `GroupsService`: `findOne(id, adminUserId)` devolve grupo com `members` + `user` (mesmo `include` que `findMany`); `update` altera nome; `remove` verifica dono, `match.count` por `groupId`, se &gt; 0 `GroupHasMatchesError`, senão `prisma.group.delete` (cascade em `GroupMember` já no schema).
    `GroupsController`: `Get(':id')`, `Patch(':id')`, `Delete(':id')` — atenção à ordem: rotas estáticas `POST :id/members` já existem; em Nest, `Get(':id')` não deve capturar `members`; garantir ordem de declaração ou path sem colisão (ex.: `Get(':id')` ok se `members` é subpath de POST apenas).
  </action>
  <acceptance_criteria>
    - `groups.controller.ts` tem GET/PATCH/DELETE em `:id` com AdminGuard
    - `groups.service.ts` lança `GroupHasMatchesError` quando existem matches
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Patch\\|Delete\\|findOne" apps/api/src/groups/groups.service.ts</automated>
  </verify>
  <done>Groups CRUD API completo.</done>
</task>

<task type="auto">
  <name>Task 4: E2E venues + groups</name>
  <files>apps/api/src/venues/venues.e2e-spec.ts</files>
  <read_first>apps/api/src/matches/matches.e2e-spec.ts, apps/api/test/e2e-default-venue.ts, apps/api/jest-e2e.config.cjs</read_first>
  <action>
    Criar `venues.e2e-spec.ts` (ou `venues-groups.e2e-spec.ts`) seguindo o padrão `describeE2e` + `beforeAll` AppModule + `loginBearer` para admin de teste dedicado (novos UUIDs constantes no ficheiro para não colidir com matches e2e).
    Casos mínimos:
    1. POST venue → GET :id → PATCH :id (nome) → GET confirma
    2. DELETE venue sem matches → 200/204
    3. POST match mínimo (reutilizar helper ou corpo igual a matches e2e) referenciando venue → DELETE venue → espera **409** e corpo com `VenueHasMatchesError`
    4. POST group → GET :id → PATCH nome → DELETE sem matches ok
    5. Criar match com `groupId` (se DTO permitir) ou skip se complexo — preferir criar match com `visibility` GROUP e `groupId` como em seed; se e2e matches já tem padrão, copiar; senão apenas testar group delete 409 com `prisma.match.create` directo no setup do teste (via prisma inject) para uma groupId — **desde que** o teste use app HTTP para DELETE.
    Limpar dados em `afterEach` com prefixos únicos (`E2E-VEN-`, `E2E-GRP-`).
  </action>
  <acceptance_criteria>
    - Ficheiro e2e existe e `pnpm --filter @peladas/api test:e2e` inclui o novo spec (via pattern jest-e2e)
    - Pelo menos um teste asserta status 409 para delete venue bloqueado
  </acceptance_criteria>
  <verify>
    <automated>cd apps/api && pnpm run test:e2e -- --testPathPattern=venues  (SKIP_E2E=1 em CI sem DB pode skip — documentar)</automated>
  </verify>
  <done>E2E verde localmente com DB.</done>
</task>

</tasks>

---
phase: 08-base-de-dados-mock-e-depend-ncias-de-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - package.json
  - apps/web-jogador/package.json
  - apps/web-jogador/.storybook/main.ts
  - apps/web-jogador/.storybook/preview.ts
  - apps/web-jogador/components/**/*.stories.tsx
  - apps/web-admin/package.json
  - apps/web-admin/.storybook/main.ts
  - apps/web-admin/.storybook/preview.ts
  - apps/web-admin/**/*.stories.tsx
  - playwright.config.ts
  - e2e/**/*.spec.ts
  - .github/workflows/ui-ci.yml
  - README.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Storybook compila estaticamente em web-jogador e web-admin com pelo menos uma story que usa @peladas/fixtures"
    - "Playwright tem pelo menos um teste smoke (ex.: carregar / ou health) com baseURL configurável por env"
    - "Workflow CI corre build dos dois Next + storybook build (E2E pode ser opcional com if: env)"
  artifacts:
    - "apps/web-jogador/.storybook/main.ts"
    - "apps/web-admin/.storybook/main.ts"
    - "playwright.config.ts"
    - ".github/workflows/ui-ci.yml"
  key_links:
    - "08-CONTEXT.md D-03 (CI)"
    - "08-VALIDATION.md"
---

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-08-03 | Misconfiguration | GitHub Actions | MITIGATE | Não gravar segredos; usar apenas `pnpm install` + build; cache pnpm opcional |
| T-08-04 | Dependency | Playwright browsers | ACCEPT | Documentar `pnpm exec playwright install` para dev local; CI usar imagem oficial ou step install |
</threat_model>

<objective>
Adicionar **Storybook** e **Playwright** consumindo `@peladas/fixtures`, e um **workflow CI** mínimo para build + storybook.

Purpose: Cumprir D-03 (fixtures na CI: dev, E2E, jobs de UI).

Output: Stories exemplo, smoke E2E, `ui-ci.yml` documentado.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-base-de-dados-mock-e-depend-ncias-de-ui/08-CONTEXT.md
@.planning/phases/08-base-de-dados-mock-e-depend-ncias-de-ui/08-RESEARCH.md
@packages/fixtures/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Storybook — web-jogador</name>
  <files>apps/web-jogador/package.json, apps/web-jogador/.storybook/main.ts, apps/web-jogador/.storybook/preview.ts</files>
  <read_first>apps/web-jogador/next.config.ts, apps/web-jogador/next.config.js</read_first>
  <action>
    Instalar Storybook 8+ para Next (`npx storybook@latest init` no diretório `apps/web-jogador` ou equivalente documentado), framework `@storybook/nextjs`.
    Adicionar scripts `"storybook"` e `"build-storybook"` em `apps/web-jogador/package.json`.
    Criar `components/ui/button.stories.tsx` (ou ao lado de `Button`) que importe texto ou prop de label e use um valor exportado de `@peladas/fixtures` (ex.: nome de cenário ou ID) apenas para provar o encadeamento.
    Garantir `pnpm --filter @peladas/web-jogador run build-storybook` gera `storybook-static`.
  </action>
  <acceptance_criteria>
    - `storybook-static` é gerado após build-storybook
    - Story referencia `@peladas/fixtures`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-jogador run build-storybook</automated>
  </verify>
  <done>Storybook jogador OK.</done>
</task>

<task type="auto">
  <name>Task 2: Storybook — web-admin</name>
  <files>apps/web-admin/package.json, apps/web-admin/.storybook/main.ts, apps/web-admin/.storybook/preview.ts</files>
  <read_first>apps/web-jogador/.storybook/main.ts</read_first>
  <action>
    Repetir configuração Storybook em `apps/web-admin` com uma story mínima (Button + fixture).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @peladas/web-admin run build-storybook` completa
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-admin run build-storybook</automated>
  </verify>
  <done>Storybook admin OK.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright na raiz</name>
  <files>package.json, playwright.config.ts, e2e/smoke.spec.ts</files>
  <read_first>apps/web-jogador/package.json</read_first>
  <action>
    Adicionar `@playwright/test` como devDependency na **raiz** do monorepo (ou num package `e2e` — preferir raiz para simplicidade).
    Criar `playwright.config.ts` com `testDir: e2e`, `baseURL: process.env.PLAYWRIGHT_BASE_URL || 'http://127.0.0.1:3002'`, `webServer` **opcional** comentado ou desactivado por defeito para não bloquear quem não tem API.
    Criar `e2e/smoke.spec.ts`: um teste que faz `page.goto('/')` e verifica status ou título **se** `PLAYWRIGHT_BASE_URL` definido; caso contrário `test.skip(true, 'PLAYWRIGHT_BASE_URL not set')` **ou** documentar que smoke requer app a correr.
    Exportar em README: `PLAYWRIGHT_BASE_URL=http://localhost:3002 pnpm exec playwright test`.
  </action>
  <acceptance_criteria>
    - `pnpm exec playwright test` termina (skipped ou green em ambiente com servidor)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec playwright test</automated>
  </verify>
  <done>Playwright configurado.</done>
</task>

<task type="auto">
  <name>Task 4: GitHub Actions — ui-ci.yml</name>
  <files>.github/workflows/ui-ci.yml, README.md</files>
  <read_first>package.json</read_first>
  <action>
    Criar workflow `ui-ci.yml` (push/PR) com job `build-frontends`: checkout, setup Node (versão alinhada ao projecto), pnpm install, `pnpm --filter @peladas/web-jogador build`, `pnpm --filter @peladas/web-admin build`, `pnpm --filter @peladas/web-jogador run build-storybook`, `pnpm --filter @peladas/web-admin run build-storybook`.
    Job E2E **opcional**: segundo job `e2e` com `if: false` ou comentário para activar quando `PLAYWRIGHT_BASE_URL` + stack estiverem definidos — evitar falhas vermelhas permanentes na primeira PR.
    README: secção "CI" com lista do que o workflow valida.
  </action>
  <acceptance_criteria>
    - Ficheiro `.github/workflows/ui-ci.yml` existe e referencia comandos reais dos package.json
  </acceptance_criteria>
  <verify>
    <automated>rg -n "build-storybook" .github/workflows/ui-ci.yml</automated>
  </verify>
  <done>CI mínima descrita.</done>
</task>

</tasks>

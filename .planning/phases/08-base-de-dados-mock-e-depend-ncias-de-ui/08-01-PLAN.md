---
phase: 08-base-de-dados-mock-e-depend-ncias-de-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - packages/fixtures/package.json
  - packages/fixtures/tsconfig.json
  - packages/fixtures/src/index.ts
  - packages/fixtures/README.md
  - apps/web-jogador/package.json
  - apps/web-jogador/tsconfig.json
  - apps/web-jogador/postcss.config.mjs
  - apps/web-jogador/tailwind.config.ts
  - apps/web-jogador/app/globals.css
  - apps/web-jogador/components.json
  - apps/web-jogador/lib/utils.ts
  - apps/web-jogador/components/ui/button.tsx
  - apps/web-admin/package.json
  - apps/web-admin/tsconfig.json
  - apps/web-admin/postcss.config.mjs
  - apps/web-admin/tailwind.config.ts
  - apps/web-admin/app/globals.css
  - apps/web-admin/components.json
  - apps/web-admin/lib/utils.ts
  - apps/web-admin/components/ui/button.tsx
  - README.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "pnpm workspace inclui packages/* e @peladas/fixtures é importável pelos dois Next.js"
    - "Ambos os apps fazem next build com Tailwind + pelo menos um componente shadcn (Button) compilando"
    - "Fixtures exportam IDs estáveis alinhados a apps/api/prisma/seed.ts (SEED_USER_ID, SEED_ADMIN_ID, etc.)"
  artifacts:
    - "packages/fixtures/src/index.ts"
    - "apps/web-jogador/tailwind.config.ts"
    - "apps/web-admin/tailwind.config.ts"
  key_links:
    - "08-CONTEXT.md D-01–D-05"
    - "08-UI-SPEC.md cores e tipografia"
---

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-08-01 | Information disclosure | packages/fixtures | MITIGATE | Apenas UUIDs e dados fictícios de dev; nunca credenciais; README a indicar uso exclusivamente local/CI |
| T-08-02 | Supply chain | shadcn CLI / deps | MITIGATE | Usar `npx shadcn@latest` oficial; lockfile pnpm; revisar `components.json` |
</threat_model>

<objective>
Criar **`@peladas/fixtures`**, integrar **Tailwind + shadcn/ui** em **`web-jogador`** e **`web-admin`**, e provar com **Button** + imports de fixtures sem alterar a API.

Purpose: Cumprir D-01–D-05 do `08-CONTEXT.md` (stack UI + pacote de fixtures + seed como fonte de verdade).

Output: Pacote fixtures publicado no workspace; ambos os apps com build verde e componente shadcn mínimo.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-base-de-dados-mock-e-depend-ncias-de-ui/08-CONTEXT.md
@.planning/phases/08-base-de-dados-mock-e-depend-ncias-de-ui/08-UI-SPEC.md
@.planning/phases/08-base-de-dados-mock-e-depend-ncias-de-ui/08-RESEARCH.md
@apps/api/prisma/seed.ts
@apps/web-jogador/app/globals.css
@apps/web-admin/app/globals.css
@pnpm-workspace.yaml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pnpm workspace — packages/*</name>
  <files>pnpm-workspace.yaml, package.json</files>
  <read_first>pnpm-workspace.yaml</read_first>
  <action>
    Em `pnpm-workspace.yaml`, além de `apps/*`, adicionar `packages/*`.
    Na raiz `package.json`, opcionalmente adicionar script `"build:frontends": "pnpm --filter @peladas/web-jogador build && pnpm --filter @peladas/web-admin build"` para verificação rápida (se já existir estrutura de scripts, não duplicar).
  </action>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` contém `packages/*`
  </acceptance_criteria>
  <verify>
    <automated>pnpm install na raiz (sem erro)</automated>
  </verify>
  <done>Workspace pronto para packages/fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: packages/fixtures — pacote tipado</name>
  <files>packages/fixtures/package.json, packages/fixtures/tsconfig.json, packages/fixtures/src/index.ts, packages/fixtures/README.md</files>
  <read_first>apps/web-jogador/lib/types.ts, apps/api/prisma/seed.ts</read_first>
  <action>
    Criar `packages/fixtures` com `name: "@peladas/fixtures"`, `private: true`, `main`/`types` apontando para `src/index.ts`.
    Em `src/index.ts`, exportar:
    - Constantes de IDs **iguais** às do seed: `SEED_USER_ID`, `SEED_ADMIN_ID`, `DEFAULT_VENUE_ID` (e outras que o seed documentar como estáveis — copiar valores literais de `seed.ts`).
    - Pelo menos um objeto exemplo `sampleMatchListItem` ou estrutura compatível com o tipo `Match` do jogador (campos que a API devolve em listagem), comentado como **exemplo estático** para Storybook/testes (não precisa corresponder a uma partida real se o objetivo for só forma).
    Não importar `@prisma/client` neste pacote.
    README: explicar que dados espelham contratos HTTP e IDs do seed.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @peladas/fixtures exec tsc --noEmit` passa (adicionar tsconfig com `strict`, `moduleResolution: bundler` ou `node16` coerente com monorepo)
    - Nenhuma string de password ou segredo nos ficheiros
  </acceptance_criteria>
  <verify>
    <automated>cd packages/fixtures; npx tsc --noEmit</automated>
  </verify>
  <done>Pacote fixtures criado.</done>
</task>

<task type="auto">
  <name>Task 3: web-jogador — Tailwind + shadcn + aliases</name>
  <files>apps/web-jogador/package.json, apps/web-jogador/tsconfig.json, apps/web-jogador/postcss.config.mjs, apps/web-jogador/tailwind.config.ts, apps/web-jogador/app/globals.css, apps/web-jogador/components.json, apps/web-jogador/lib/utils.ts, apps/web-jogador/components/ui/button.tsx</files>
  <read_first>apps/web-jogador/app/layout.tsx, 08-UI-SPEC.md</read_first>
  <action>
    Instalar dependências: `tailwindcss`, `postcss`, `autoprefixer`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `@radix-ui/react-slot` (conforme gerado pelo shadcn).
    Adicionar a `tsconfig.json`: `"paths": { "@/*": ["./*"] }` com `baseUrl: "."` se necessário.
    Executar `npx shadcn@latest init` na pasta `apps/web-jogador` com opções não interactivas aceites pelo CLI (ex.: defaults); garantir `tailwind.css` aponta para `app/globals.css`, `rsc: true`.
    No topo de `app/globals.css`, incluir directivas Tailwind **antes** de preservar variáveis legadas em `:root` onde fizer sentido; mapear cores do UI-SPEC para variáveis shadcn (`--background`, `--primary`, etc.) para não regressão visual grave.
    Executar `npx shadcn@latest add button`.
    Adicionar dependência workspace: `"@peladas/fixtures": "workspace:*"` em `apps/web-jogador/package.json`.
    Usar `<Button>` num local mínimo (ex.: `components/organisms` ou header existente) **ou** página de desenvolvimento temporária — apenas para provar import; não remover funcionalidade existente.
  </action>
  <acceptance_criteria>
    - `apps/web-jogador/components/ui/button.tsx` existe
    - `pnpm --filter @peladas/web-jogador build` completa sem erro
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-jogador build</automated>
  </verify>
  <done>web-jogador com shadcn operacional.</done>
</task>

<task type="auto">
  <name>Task 4: web-admin — Tailwind + shadcn (espelhar jogador)</name>
  <files>apps/web-admin/package.json, apps/web-admin/tsconfig.json, apps/web-admin/postcss.config.mjs, apps/web-admin/tailwind.config.ts, apps/web-admin/app/globals.css, apps/web-admin/components.json, apps/web-admin/lib/utils.ts, apps/web-admin/components/ui/button.tsx</files>
  <read_first>apps/web-jogador/tailwind.config.ts, apps/web-jogador/components.json</read_first>
  <action>
    Repetir a abordagem da Task 3 em `apps/web-admin` (Tailwind, shadcn init, `add button`, paths `@/*`, dependência `@peladas/fixtures`).
    Alinhar tokens de cor ao `08-UI-SPEC.md` como no jogador.
    Integrar `<Button>` num ponto mínimo da UI admin existente.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @peladas/web-admin build` completa sem erro
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/web-admin build</automated>
  </verify>
  <done>web-admin com shadcn operacional.</done>
</task>

<task type="auto">
  <name>Task 5: README — seed vs fixtures</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
    Documentar em secção curta: (1) `db:seed` continua a ser a fonte de dados com Postgres; (2) `@peladas/fixtures` serve testes/UI com **mesmos IDs**; (3) comandos `pnpm --filter @peladas/fixtures` / import em apps.
  </action>
  <acceptance_criteria>
    - README menciona `@peladas/fixtures` e alinhamento com `seed.ts`
  </acceptance_criteria>
  <verify>
    <automated>rg "@peladas/fixtures" README.md</automated>
  </verify>
  <done>Documentação actualizada.</done>
</task>

</tasks>

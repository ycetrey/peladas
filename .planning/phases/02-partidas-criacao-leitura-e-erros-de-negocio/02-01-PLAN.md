---
phase: 02-partidas-criacao-leitura-e-erros-de-negocio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/main.ts
  - apps/api/src/common/errors
  - apps/api/src/common/filters
  - apps/api/package.json
autonomous: true
requirements:
  - ERR-01
user_setup: []
must_haves:
  truths:
    - "Erros de domínio serializam JSON { error: { code, message } }"
    - "Filtro global Nest aplica-se a DomainError ou interface comum"
  artifacts:
    - apps/api/src/common/filters/domain-exception.filter.ts
    - apps/api/src/common/errors/domain-errors.ts
  key_links:
    - ".planning/phases/02-partidas-criacao-leitura-e-erros-de-negocio/02-CONTEXT.md D-03"
---

<objective>
Introduzir **infraestrutura de erros de negócio** (ERR-01): classes de erro nomeadas, resposta JSON estável e `ExceptionFilter` registado na aplicação Nest.

Purpose: Desbloquear o módulo de partidas (plano 02-02) com contrato de erro único.

Output: ficheiros em `apps/api/src/common/`, alteração a `main.ts`, teste unitário mínimo opcional mas recomendado.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-partidas-criacao-leitura-e-erros-de-negocio/02-CONTEXT.md
@.planning/intel/constraints.md
@apps/api/src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain error classes</name>
  <files>apps/api/src/common/errors/domain-errors.ts</files>
  <read_first>
    - `.planning/phases/02-partidas-criacao-leitura-e-erros-de-negocio/02-CONTEXT.md`
    - `.planning/intel/constraints.md` (C-SPEC-005)
  </read_first>
  <action>
    Criar `apps/api/src/common/errors/domain-errors.ts` com classe base (ex.: `DomainError` extends `Error`) propriedade `readonly code: string` igual ao nome da classe (ex.: `"MatchNotOpenError"`).
    Exportar classes concretas (cada uma `extends DomainError` com mensagem default): `MatchNotOpenError`, `RegistrationClosedError`, `UserAlreadyRegisteredError`, `InvalidMatchStateError`, `MatchNotFoundError`, `InvalidMatchCreationError` (últimas duas para fase 2; as outras para contrato ERR-01 global mesmo que só usadas na fase 3).
  </action>
  <acceptance_criteria>
    - `apps/api/src/common/errors/domain-errors.ts` existe e contém as strings `MatchNotOpenError` e `MatchNotFoundError`
    - Cada classe pública tem `readonly code` ou getter `code` documentado
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec nest build</automated>
  </verify>
  <done>Tipos de erro nomeados disponíveis para o filtro e serviços.</done>
</task>

<task type="auto">
  <name>Task 2: DomainExceptionFilter + bootstrap</name>
  <files>apps/api/src/common/filters/domain-exception.filter.ts, apps/api/src/main.ts</files>
  <read_first>
    - `apps/api/src/main.ts`
    - `apps/api/src/common/errors/domain-errors.ts`
  </read_first>
  <action>
    Implementar `DomainExceptionFilter` que captura `DomainError` (ou verifica `instanceof`) e responde com status HTTP fixo **400** (ou 404 só para `MatchNotFoundError`, 409 para conflitos se preferir — documentar na resposta do README na fase 02-02; mínimo: `MatchNotFoundError` → 404, restantes DomainError → 400) e corpo exatamente `{"error":{"code":"<code>","message":"<message>"}}`.
    Em `main.ts`: `app.useGlobalFilters(new DomainExceptionFilter())` após criar `NestFactory`.
    Garantir `ValidationPipe` não mascara o filtro — erros de validação Nest podem usar outro formato; opcionalmente alinhar num segundo passo.
  </action>
  <acceptance_criteria>
    - `domain-exception.filter.ts` contém a substring `"error"` e `"code"`
    - `main.ts` contém `useGlobalFilters`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec nest build</automated>
  </verify>
  <done>ERR-01: formato estável ligado ao pipeline HTTP.</done>
</task>

<task type="auto">
  <name>Task 3: Jest + unit smoke for DomainError response</name>
  <files>apps/api/package.json, apps/api/src/common/filters/domain-exception.filter.spec.ts</files>
  <read_first>
    - `apps/api/package.json`
  </read_first>
  <action>
    Adicionar devDependencies: `jest`, `ts-jest`, `@types/jest`, `@nestjs/testing`, `supertest`, `@types/supertest` (versões compatíveis com Nest 10 / Node 22).
    Adicionar script `"test": "jest"` e ficheiro `jest.config` mínimo ou config inline em package.json para compilar TS de `src/`.
    Criar `domain-exception.filter.spec.ts` que instancia o filtro (ou via TestingModule mínimo) e verifica que `MatchNotFoundError` produz resposta JSON com `error.code` === `MatchNotFoundError`.
  </action>
  <acceptance_criteria>
    - `apps/api/package.json` contém `"test":`
    - `pnpm --filter @peladas/api exec jest domain-exception.filter.spec.ts` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec jest domain-exception.filter.spec.ts</automated>
  </verify>
  <done>Wave 0 mínimo para Nyquist.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Erros não devem incluir stack trace em JSON de resposta |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | Error JSON | mitigate | Apenas `code` + `message`; stack só em logs servidor se `NODE_ENV!==production` |
| T-02-02 | E | Global filter | mitigate | Não vazar detalhes de DB em mensagens de DomainError |
</threat_model>

<verification>
- `pnpm --filter @peladas/api exec nest build`
- `pnpm --filter @peladas/api exec jest domain-exception.filter.spec.ts`
</verification>

<success_criteria>
Filtro global ativo; erros de domínio devolvem JSON com `error.code` estável; teste Jest passa.
</success_criteria>

<output>
Após conclusão: `.planning/phases/02-partidas-criacao-leitura-e-erros-de-negocio/02-01-SUMMARY.md`
</output>

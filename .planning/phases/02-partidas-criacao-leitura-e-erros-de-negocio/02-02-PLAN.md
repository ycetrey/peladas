---
phase: 02-partidas-criacao-leitura-e-erros-de-negocio
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/api/src/matches/match-rules.service.ts
  - apps/api/src/matches/match-rules.service.spec.ts
autonomous: true
requirements:
  - RULE-01
user_setup: []
must_haves:
  truths:
    - "MatchRulesService rejeita maxPlayers ímpar e janelas inválidas"
    - "Testes unitários cobrem casos felizes e de erro"
  artifacts:
    - apps/api/src/matches/match-rules.service.ts
    - apps/api/src/matches/match-rules.service.spec.ts
  key_links:
    - ".planning/intel/constraints.md C-SPEC-007"
---

<objective>
Implementar **MatchRulesService** com validações RULE-01 e **testes unitários** no mesmo plano (ficheiro spec listado em `<files>` e criado em `<action>`).

Purpose: Isolar regras de criação testáveis antes do HTTP (plano 02-03).

Output: `match-rules.service.ts` + `match-rules.service.spec.ts` a verde com `jest`.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-partidas-criacao-leitura-e-erros-de-negocio/02-CONTEXT.md
@apps/api/prisma/schema.prisma
@.planning/intel/constraints.md
@apps/api/src/common/errors/domain-errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MatchRulesService + unit tests</name>
  <files>apps/api/src/matches/match-rules.service.ts, apps/api/src/matches/match-rules.service.spec.ts</files>
  <read_first>
    - `.planning/intel/constraints.md` (C-SPEC-007)
    - `apps/api/prisma/schema.prisma` (model Match)
    - `apps/api/src/common/errors/domain-errors.ts`
  </read_first>
  <action>
    Criar `MatchRulesService` com `validateCreate(input)` onde `input` tem os campos de criação (datas, maxPlayers, maxSubstitutes, mode, recurringRule opcional).
    Regras: `registrationOpensAt < registrationClosesAt < dateTime`; `maxPlayers` par e `>= 2`; `maxSubstitutes >= 0`; `mode` válido; `recurringRule` opcional validado minimamente (C-SPEC-002: `frequency` enum).
    Em falha lançar `InvalidMatchCreationError` com mensagem descritiva.
    Criar `match-rules.service.spec.ts` com pelo menos: (1) criação válida não lança; (2) `maxPlayers` ímpar lança; (3) `registrationClosesAt >= dateTime` lança.
  </action>
  <acceptance_criteria>
    - Ambos os ficheiros listados em `<files>` existem após a tarefa
    - `match-rules.service.ts` contém `% 2` ou equivalente para paridade
    - `match-rules.service.spec.ts` contém pelo menos duas chamadas `expect(` com `toThrow` ou similar
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec jest match-rules.service.spec.ts</automated>
  </verify>
  <done>RULE-01 com cobertura automática.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Rules only | Sem I/O HTTP; sem dados externos |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | T | Validation | accept | Lógica pura; testes impedem regressão |
</threat_model>

<verification>
pnpm --filter @peladas/api exec jest match-rules.service.spec.ts
pnpm --filter @peladas/api exec nest build
</verification>

<success_criteria>
Jest verde; build Nest OK.
</success_criteria>

<output>
Após conclusão: `.planning/phases/02-partidas-criacao-leitura-e-erros-de-negocio/02-02-SUMMARY.md`
</output>

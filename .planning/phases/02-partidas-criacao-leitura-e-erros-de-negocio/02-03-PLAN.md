---
phase: 02-partidas-criacao-leitura-e-erros-de-negocio
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/api/src/matches
  - apps/api/src/app.module.ts
  - README.md
autonomous: true
requirements:
  - MAT-01
  - MAT-02
  - API-01
  - API-02
  - API-03
user_setup: []
must_haves:
  truths:
    - "POST /matches cria Match com status OPEN"
    - "GET /matches e GET /matches/:id funcionam; 404 para id inexistente"
  artifacts:
    - apps/api/src/matches/matches.controller.ts
    - apps/api/src/matches/matches.e2e-spec.ts
  key_links:
    - "02-CONTEXT.md D-01 cabeçalho organizador"
---

<objective>
**HTTP + Prisma** para partidas: guard de organizador, `POST /matches`, `GET /matches`, `GET /matches/:id`, teste e2e, README.

Purpose: Fechar MAT-*, API-* após regras (02-02) e erros (02-01).

Output: `MatchesModule`, e2e spec listado em `<files>`, documentação.
</objective>

<execution_context>
@D:/Fontes/peladas/.cursor/get-shit-done/workflows/execute-plan.md
@D:/Fontes/peladas/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-partidas-criacao-leitura-e-erros-de-negocio/02-CONTEXT.md
@apps/api/src/health/health.module.ts
@apps/api/src/prisma/prisma.service.ts
@apps/api/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Organizer guard + MatchesModule skeleton</name>
  <files>apps/api/src/matches/organizer-user.guard.ts, apps/api/src/matches/matches.module.ts, apps/api/src/matches/matches.service.ts, apps/api/src/app.module.ts</files>
  <read_first>
    - `apps/api/src/health/health.module.ts`
    - `apps/api/src/prisma/prisma.service.ts`
    - `.planning/phases/02-partidas-criacao-leitura-e-erros-de-negocio/02-CONTEXT.md`
  </read_first>
  <action>
    Criar `OrganizerUserGuard` (validar `x-organizer-user-id`, UUID, `User` existe via Prisma; senão `UnauthorizedException`).
    Criar `MatchesModule` importando `PrismaModule`, providers `MatchesService`, `MatchRulesService`, `OrganizerUserGuard`; exportar o que for necessário ao controller.
    `MatchesService` stub com injeção de `PrismaService` e `MatchRulesService`.
    Registar `MatchesModule` em `AppModule.imports`.
  </action>
  <acceptance_criteria>
    - `organizer-user.guard.ts` e `matches.module.ts` existem
    - `app.module.ts` contém `MatchesModule`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec nest build</automated>
  </verify>
  <done>Infra HTTP preparada.</done>
</task>

<task type="auto">
  <name>Task 2: Controller POST + GET + Prisma persistence</name>
  <files>apps/api/src/matches/dto/create-match.dto.ts, apps/api/src/matches/matches.controller.ts, apps/api/src/matches/matches.service.ts</files>
  <read_first>
    - `apps/api/src/matches/match-rules.service.ts`
    - `.planning/intel/constraints.md` (C-SPEC-004)
    - `apps/api/prisma/schema.prisma`
  </read_first>
  <action>
    `CreateMatchDto` com validação class-validator onde aplicável; datas como ISO 8601 string.
    `MatchesService.create`: chamar `matchRules.validateCreate`, `prisma.match.create` com `status: OPEN` (D-06).
    `MatchesController`: `@Controller('matches')`; `@Post()` com `@UseGuards(OrganizerUserGuard)` + body DTO → 201 `{ "match": {...} }`.
    `@Get()` **antes** de `@Get(':id')` — lista com query `status`, `limit`; `@Get(':id')` detalhe ou `MatchNotFoundError`.
    Implementar `findMany` / `findOne` no service.
  </action>
  <acceptance_criteria>
    - `matches.controller.ts` declara `@Get()` antes de `@Get(':id')`
    - `matches.service.ts` contém `prisma.match.create` e string `OPEN`
    - `create-match.dto.ts` existe
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec nest build</automated>
  </verify>
  <done>MAT-01, MAT-02, API-01..03.</done>
</task>

<task type="auto">
  <name>Task 3: E2E spec + README</name>
  <files>apps/api/src/matches/matches.e2e-spec.ts, README.md</files>
  <read_first>
    - `apps/api/src/matches/matches.controller.ts`
    - `apps/api/package.json` (script test / jest)
    - `README.md`
    - `apps/api/prisma/seed.ts` (UUIDs dos users seed para usar no header)
  </read_first>
  <action>
    Criar `matches.e2e-spec.ts` com `Test.createTestingModule` + `supertest` (ou padrão Nest e2e): caso feliz `POST /matches` com header do seed admin/jogador; `GET /matches`; `GET /matches/:id`; expectativa de status e presença de `match.id` ou lista.
    Se e2e exigir DB, usar `DATABASE_URL` de teste documentado ou `sqlite` — **preferir** documentar execução com Postgres compose e `jest` com setup que corre contra BD de dev (aceitar skip condicional `SKIP_E2E` env só se documentado).
    Atualizar `README.md` com secção API Partidas e exemplos `curl` + `X-Organizer-User-Id`.
  </action>
  <acceptance_criteria>
    - `matches.e2e-spec.ts` existe e referencia `POST` ou `/matches`
    - `README.md` contém `X-Organizer-User-Id` e `POST`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @peladas/api exec jest matches.e2e-spec.ts</automated>
  </verify>
  <done>Smoke integração + docs.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /matches | Header validado contra utilizadores reais |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | I | GET público | accept | Metadados de partida apenas |
| T-02-04 | T | Organizer header | mitigate | UUID + existência em DB |
</threat_model>

<verification>
pnpm --filter @peladas/api exec nest build
pnpm --filter @peladas/api exec jest matches.e2e-spec.ts
</verification>

<success_criteria>
E2E verde com DB configurado; README atualizado.
</success_criteria>

<output>
Após conclusão: `.planning/phases/02-partidas-criacao-leitura-e-erros-de-negocio/02-03-SUMMARY.md`
</output>
